interface:
  display_name: "Fast Forward Mode"
  short_description: "Deliver verified batches with minimal ceremony"
  default_prompt: "Use $fast-forward-mode to move this project to its next runnable or releasable milestone in coherent, verified batches."
