# go2rtc Local Development Configuration
api:
  listen: :1984
  origin: '*'

webrtc:
  candidates:
    - stun:8555
  listen: :8555

log:
  format: text
  level: info

# Streams are created on-demand via PUT /api/streams from the app
streams: {}
