# Listen delivery contract

`v1/README.md` is the current canonical delivery and recovery entry point for
the isolated Listen service. It preserves the `early-birds` lane and wraps the
existing Listener lifecycle; it does not replace Account delivery or authorize
production activation.

Historical launch handoffs and Telegram prose are retained as history only.
They are not current artifact, alert-recipient, Authority-contract, delivery,
or recovery evidence under this contract.
