# Copy to /etc/harmonic-beacon/account.staging-database.env as root:root 0600.
# This file is injected only into the dedicated staging PostgreSQL container.
POSTGRES_USER=beacon_account_staging
POSTGRES_PASSWORD=replace-staging-database-password-32-random
POSTGRES_DB=beacon_account_staging
