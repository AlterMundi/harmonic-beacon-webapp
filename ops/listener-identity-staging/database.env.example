# Root-only staging database bootstrap. Values must match DATABASE_URL in the
# application environment. Never reuse preview or production credentials.
POSTGRES_USER=listener_identity_staging
POSTGRES_PASSWORD=replace-listener-identity-staging-database-password
POSTGRES_DB=listener_identity_staging
