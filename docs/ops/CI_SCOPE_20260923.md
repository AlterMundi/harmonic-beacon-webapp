# Bounded CI scope improvement

Mariano authorized reducing unnecessary verification latency on2026-09-23.
Separate from PR583 (which includes PR581): do not change its required gate
to admit that candidate.

## Plan

1. Preserve unknown-path fail-closed behavior, critical-domain matrices and exact
   head/base qualification. No skipped required checks or status reuse.
2. Classify the two reviewed read-only cohort-export tools explicitly as operator
   observation tooling. They read production and write a private local snapshot;
   they do not participate in app, worker, audio or payment runtime. Their own
   Python tests must run in the existing ops-tooling job.
3. Test isolated export, mixed app/export, and mixed identity/payment/audio/schema
   changes. Adding a recognized read-only file must never subtract checks imposed
   by another changed file. Similar but unreviewed filenames stay unknown.
4. Merge after PR583 so no current candidate self-authorizes reduced evidence.
   This changes future selection, not production containers or privileges.
5. Keep local iteration focused; publish coherent batches instead of pushing each
   test adjustment. Hosted exact-candidate checks run once the batch is ready.

This first change removes one demonstrated false expansion. It does not promise
to make every app change fast or replace browser regression with unit tests.
Broader browser-profile splitting needs measured coverage and a separate design;
it is not bundled into this correction.
